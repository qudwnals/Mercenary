spring:
  datasource:
    url: jdbc:mysql://localhost:3307/mercenary?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  data:
    redis:
      host: localhost
      port: 6379

# Swagger ?? (?? ??: http://localhost:8080/swagger-ui/index.html)
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

jwt:
   # 최소 32글자 이상의 랜덤한 문자열
 secret: mercenary_high_project_secret_key_for_jwt_2025_fighting

kakao:
  client-id: 7d14f9ab2e737ea77a60f2c1bffce860
